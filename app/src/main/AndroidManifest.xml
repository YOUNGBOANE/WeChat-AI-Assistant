<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposed_desc" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ChatListActivity"
            android:exported="false"
            android:label="@string/menu_chats" />

        <activity
            android:name=".PromptActivity"
            android:exported="false"
            android:label="@string/menu_prompts"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".KeywordActivity"
            android:exported="false"
            android:label="@string/menu_keywords"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ConfigActivity"
            android:exported="false"
            android:label="@string/menu_backup" />

        <activity
            android:name=".ModelActivity"
            android:exported="false"
            android:label="@string/menu_models"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ChatActivity"
            android:exported="false" />

        <activity
            android:name=".MemoryActivity"
            android:exported="false"
            android:label="@string/menu_memory"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".LogActivity"
            android:exported="false"
            android:label="@string/menu_logs" />

        <provider
            android:name=".ingest.KeyProvider"
            android:authorities="com.wxplain.app.key"
            android:exported="true" />

        <provider
            android:name=".ingest.AssistantProvider"
            android:authorities="com.wxplain.app.assistant"
            android:exported="true" />
    </application>
</manifest>
